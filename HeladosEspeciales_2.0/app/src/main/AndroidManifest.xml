<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.erick.heladosespeciales">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".postres_base_helado">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".tipos_helados" />
        <activity android:name=".sesion" />
        <activity android:name=".Receta_Helado" />
        <activity android:name=".Registrar" />
        <activity android:name=".Comentario"></activity>
    </application>

</manifest>